# Last Modified: Tue Jun 13 10:54:33 2017
#include <tunables/global>

/usr/bin/quasselclient flags=(attach_disconnected) {
  #include <abstractions/audio>
  #include <abstractions/base>
  #include <abstractions/bash>
  #include <abstractions/consoles>
  #include <abstractions/dbus-session-strict>
  #include <abstractions/fonts>
  #include <abstractions/freedesktop.org>
  #include <abstractions/kde>
  #include <abstractions/nameservice>
  #include <abstractions/openssl>
  #include <abstractions/python>
  #include <abstractions/ubuntu-browsers.d/plugins-common>

  / r,
  /bin/egrep r,
  /bin/uname r,
  /dev/ r,
  /dev/dri/ r,
  /etc/udev/udev.conf r,
  /home/ r,
  /proc/*/fd/ r,
  /run/udev/data/+pci* r,
  /sys/devices/pci**/config r,
  /sys/devices/pci**/uevent r,
  /sys/devices/pci*/*/revision r,
  /sys/devices/pci*/*/vendor r,
  /tmp/orcexec.* m,
  /usr/bin/dbus-send r,
  /usr/bin/pulseaudio Px,
  /usr/bin/quasselclient mr,
  /usr/bin/xdg-open rix,
  /usr/bin/xprop r,
  /usr/lib64/gstreamer-1.0/gst-plugin-scanner Px,
  /usr/share/apps/quassel/stylesheets/*.qss r,
  /usr/share/apps/quassel/translations/en_US.qm r,
  /usr/share/config/kdebug.areas r,
  /usr/share/config/kdebugrc r,
  /usr/share/cursors/** r,
  /usr/share/icons/** rk,
  /usr/share/quassel/stylesheets/*.qss r,
  /usr/share/quassel/translations/ r,
  /usr/share/quassel/translations/*.qm r,
  /var/tmp/kdecache-amir/icon-cache.kcache rw,
  /{usr/,}lib{,32,64}/** mr,
  owner @{HOME}/.Xauthority r,
  owner @{HOME}/.cache/gstreamer-1.0/ w,
  owner @{HOME}/.cache/gstreamer-1.0/registry.x86_64.bin rw,
  owner @{HOME}/.cache/gstreamer-1.0/registry.x86_64.bin.tmp* rw,
  owner @{HOME}/.cache/qt_compose_cache_* rw,
  owner @{HOME}/.config/Trolltech.conf rk,
  owner @{HOME}/.config/fontconfig/fonts.conf r,
  owner @{HOME}/.config/pulse/ rw,
  owner @{HOME}/.config/pulse/** rwk,
  owner @{HOME}/.config/quassel-irc.org/.* rwk,
  owner @{HOME}/.config/quassel-irc.org/quasselclient.conf* rwk,
  owner @{HOME}/.config/quassel-irc.org/settings.qss rwk,
  owner @{HOME}/.kde4/share/config/kdebugrc r,
  owner @{HOME}/.kde4/share/config/kdeglobals r,
  owner @{HOME}/.kde4/share/config/quasselrc rw,
  owner @{HOME}/.kde4/share/config/quasselrc*.new rwk,
  owner @{HOME}/.pulse rw,
  owner @{HOME}/.pulse/** rwk,
  owner @{HOME}/orcexec.* mrw,
  owner @{HOME}/sound/ r,
  owner @{HOME}/sound/** r,

}
